<p id="notice"><%= notice %></p>

<h1>Sports</h1>

<table class="table table-bordered table-striped" id='server-side-sport'>

</table>
<% if current_user && current_user.admin? %>
  <%= link_to 'New Sport', new_sport_path, class:"btn btn-secondary" %>
<% end %>
