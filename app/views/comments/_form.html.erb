<%= form_with(model: [@sport,@post,comment], local: true) do |form| %>
  <% if comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
  <%= form.label :post %>
  <%= form.collection_select(:post_id, Post.all, :id, :title, { :prompt => 'Select a Post', :selected => @comment.post_id }, { class: 'form-control js-example-basic-single' }) %>
  </div>

   <div class="field form-group">
    <%= form.text_field :user_id, value: current_user.id,type: :hidden,class:"form-control" %>
  </div>
  
  <div class="field form-group">
    <%= form.label :comment %>
    <%= form.text_field :comment, class:"form-control"%>
  </div>

  <div class="field">
  <%= form.label :hashtag %></br>
  <%= form.text_area :content %>
  </div>

  <br>
  <div class="actions">
    <%= form.submit class:"btn btn-secondary" %>
  </div>
<% end %>
