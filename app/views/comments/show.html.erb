<p id="notice"><%= notice %></p>

<p>
  <strong>Post:</strong>
  <%= @comment.post.title %>
</p>

<p>
  <strong>Comment:</strong>
  <%= @comment.comment %>
</p>

<p>
  <strong>tag:</strong>
</p>

<%= render partial: "hashtags/form", locals: {tagable: @comment} %>
<%= link_to 'Edit', edit_sport_post_comment_path(@sport,@post, @comment), class:"btn btn-secondary" %> |
<%= link_to 'Back', sport_post_comments_path(@sport,@post), class:"btn btn-secondary" %>
