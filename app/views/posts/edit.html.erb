<h1>Editing Post</h1>

<%= render 'form', post: @post %>
<br>
<%= link_to 'Show', [@sport, @post], class:"btn btn-secondary"%> 
<%= link_to 'Back', sport_posts_path(@sport), class:"btn btn-secondary" %>
