<%= form_with(model: achievement,:html => {id: "achievemetValidate"}, local: true) do |form| %>
  <% if achievement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(achievement.errors.count, "error") %> prohibited this achievement from being saved:</h2>

      <ul>
      <% achievement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= form.label :player %>
    <%= form.text_field :player, class:"form-control" %>
  </div>

  <div class="field form-group">
    <%= form.label :award %>
    <%= form.text_field :award, class:"form-control" %>
  </div>

  <div class="field form-group">
    <%= form.label :medal %>
    <%= form.text_field :medal, class:"form-control" %>
  </div>
  
   <div class="field form-group">
    <%= form.text_field :user_id, value: current_user.id,type: :hidden,class:"form-control" %>
  </div>
  <% if current_user.admin?%>
    <div class="field">
      <%= form.label :Staus %>
      approve<%= form.radio_button :status, ['approve']%>
      reject<%= form.radio_button :status, ['reject']%>
  </div>
  <%end%>
 <br>
  <div class="actions">
    <%= form.submit class:'btn btn-secondary'%>
  </div>
<% end %>
